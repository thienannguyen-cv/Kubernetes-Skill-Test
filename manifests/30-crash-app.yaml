apiVersion: apps/v1
kind: Deployment
metadata:
  name: crash-app
  namespace: skill-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crash-app
  template:
    metadata:
      labels:
        app: crash-app
    spec:
      containers:
        - name: app
          image: busybox
          command: ["sh", "-c", "exit 1"]
